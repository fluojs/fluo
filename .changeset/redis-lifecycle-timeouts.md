---
"@fluojs/redis": patch
---

Bound lifecycle-owned Redis `connect()` and `quit()` calls with configurable timeouts, document dedicated Pub/Sub subscriber connections, and preserve command error propagation through regression coverage.
