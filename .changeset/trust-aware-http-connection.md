---
"@fluojs/http": minor
"@fluojs/runtime": minor
"@fluojs/throttler": minor
---

Add immutable, trust-aware HTTP connection resolution with explicit hop, CIDR, and predicate proxy policies. Node-backed adapters now snapshot portable transport metadata, and rate-limit consumers can migrate from `trustProxyHeaders` to `trustProxy`.
