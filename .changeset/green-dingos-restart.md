---
"@fluojs/cli": patch
---

Keep generated starter CLI scripts aligned to the generator version and bound `fluo dev` restart shutdowns so non-cooperative child processes cannot hang restarts indefinitely.
