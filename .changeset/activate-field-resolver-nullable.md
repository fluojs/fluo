---
"@fluojs/graphql": minor
---

Activate `@FieldResolver({ nullable: false, type })` for newly added object fields, exposing them as non-null GraphQL outputs without changing existing field nullability.
