---
'@fluojs/runtime': minor
---

Add portable `parseMultipartStream(...)` multipart consumption with typed field/file parts, bounded streaming limits, cancellation propagation, and single-consumer protection.
