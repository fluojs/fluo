---
"@fluojs/react": minor
---

Add `@Router(...)` and `@Path(...)` decorators that write existing HTTP controller and `GET` route metadata while recording React-specific marker and render metadata for diagnostics and future rendering integration.
