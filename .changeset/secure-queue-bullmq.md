---
"@fluojs/queue": patch
---

Raise BullMQ to 5.81.1 or newer so consumers install its patched dependency graph. Refresh application lockfiles when upgrading; queue registration, worker discovery, and persisted-job contracts are unchanged.
