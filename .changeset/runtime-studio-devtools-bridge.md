---
"@fluojs/runtime": minor
---

Expose `@fluojs/runtime/devtools` and an explicit `studio` bootstrap bridge for host-owned Studio transports while retaining the CLI-injected Node configuration default.
