---
"@fluojs/i18n": minor
---

Add opt-in generated i18n type helper declarations so applications can type fully qualified translation keys and namespace-scoped key facades without narrowing the runtime `I18nService.translate(key: string, ...)` method.
