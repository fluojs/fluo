---
"@fluojs/cli": major
---

Remove monorepo-local starter dependency overrides from the documented `NewCommandRuntimeOptions` public type while keeping the sandbox harness behavior internal to the CLI implementation.
