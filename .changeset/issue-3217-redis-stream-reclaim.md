---
"@fluojs/microservices": major
---

Reclaim abandoned Redis Streams request and event entries with `XAUTOCLAIM` after the configured idle interval, preventing pending entries from remaining stranded after a consumer crashes.
