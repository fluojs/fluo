---
"@fluojs/testing": patch
---

Harden testing portability and conformance harness cleanup behavior, enforce non-empty platform diagnostic messages, add web-runtime exact raw-byte parity coverage, and mark `@babel/core` as an optional peer required only for the Vitest plugin entrypoint.
