---
"@fluojs/runtime": patch
---

Clarify the shipped English and Korean README examples: `@Get()` uses an empty
relative path while preserving controller prefixes, and an empty `@Module()` can
bootstrap and close without providers.
