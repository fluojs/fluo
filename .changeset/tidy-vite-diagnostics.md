---
"@fluojs/vite": patch
---

Keep lazy Babel peer diagnostics file-specific after a failed import, document the Node.js/Vite/Babel runtime contract in the Vite package and reference docs, and add regression coverage for bare plugin creation, successful lazy-load reuse, and concurrent eligible transforms.
