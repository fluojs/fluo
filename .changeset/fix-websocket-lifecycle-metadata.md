---
"@fluojs/websockets": minor
---

Preserve fetch-style host bindings through shutdown, close and settle sockets after terminal errors or failed room sends, isolate inherited gateway metadata, expose socket IDs to message handlers, and add opt-in event-envelope handler replies while preserving ignored returns by default.
