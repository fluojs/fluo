---
"@fluojs/http": patch
"@fluojs/react": minor
---

Add the React Web Streams SSR core so React page handlers can return `ReactServerEntry` values that preserve the existing HTTP pipeline before streamed HTML finalization.
