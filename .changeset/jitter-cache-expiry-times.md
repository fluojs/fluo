---
'@fluojs/cache-manager': minor
---

Add opt-in bounded TTL jitter at the `CacheService` store-handoff boundary, including deterministic randomness injection, per-call TTL override support, and consistent memory, Redis, and custom-store behavior.
