---
"@fluojs/react": minor
---

Add `ReactModule.forRoot({ controllers: [...] })` so React routers register through the existing fluo module and HTTP handler metadata path without introducing a separate React URL matcher.
