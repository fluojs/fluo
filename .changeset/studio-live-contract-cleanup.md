---
"@fluojs/studio": patch
---

Expand Studio live event contract coverage, abort stale sidecar state replay during cleanup, document `isStudioLiveEvent(value)`, and align Studio docs with the shipped `fluo dev --studio` live devtool plus static artifact compatibility.
