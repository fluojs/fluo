---
'@fluojs/drizzle': patch
---

Export documented raw Drizzle DI tokens from `DrizzleModule` so importing and global sibling modules can inject the configured database, dispose hook, and normalized runtime options.
