---
"@fluojs/websockets": patch
---

Raise the package-owned `ws` dependency to 8.21.0 or newer for the Node.js WebSocket runtime. Refresh consumer lockfiles when upgrading so the patched runtime is installed.
