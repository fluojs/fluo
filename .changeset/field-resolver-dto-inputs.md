---
"@fluojs/graphql": minor
---

Add `@Args()` and `FieldResolverOptions.input` so code-first object field resolvers can materialize and validate DTO-bound GraphQL arguments at explicit TC39 decorator indexes.
