---
"@fluojs/config": patch
---

Keep ConfigModule watch bootstrap aligned with the injected ConfigService baseline, watch parent directories for atomic env-file replacements, and expose an onReloadError hook for automatic watch reload failures.
