---
"@fluojs/cli": minor
---

Simplify the generated React SSR + Vite starter so the first editable page is isolated from manifest, renderer, document, route-snapshot, and hydration wiring while preserving explicit HTTP routes and replaceable application-owned server/client composition.
