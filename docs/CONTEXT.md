# fluo — AI Context Document

This document is the primary AI-reference entrypoint for the fluo repository. It summarizes framework identity, non-negotiable authoring rules, package boundaries, and the fastest path to the correct source document.

## Identity

fluo is a standard-first TypeScript backend framework built on TC39 standard decorators, explicit dependency boundaries, and metadata-free runtime wiring. It rejects legacy decorator compiler modes and treats behavioral contracts, platform parity, and package surface clarity as core design constraints.

## Hard Constraints

- NEVER use `experimentalDecorators`.
- NEVER use `emitDecoratorMetadata`.
- NEVER access `process.env` directly inside packages, use `@fluojs/config` at the application boundary.
- Platform packages MUST implement the repository policy seam named `PlatformAdapter`; current HTTP adapters satisfy it through `HttpApplicationAdapter` from `@fluojs/http`.
- All public exports MUST have TSDoc.
- Breaking changes in `1.0+` MUST trigger a major version bump.

## Contribution Quickstart

For the full contributor workflow, read [`CONTRIBUTING.md`](../CONTRIBUTING.md) and use [`.github/PULL_REQUEST_TEMPLATE.md`](../.github/PULL_REQUEST_TEMPLATE.md) when preparing a PR.

- AI-assisted PRs from tools such as Codex or Claude are welcome when the PR preserves the original context.
- Issues are encouraged when a problem needs discussion, but they are not required for focused fixes, documentation updates, or clearly scoped improvements. If there is no issue, summarize the problem and intended outcome in the PR.
- Target `main` and run `pnpm verify` before opening or updating a PR when possible.
- Public export changes under `packages/*/src` must include source TSDoc and pass `pnpm verify:public-export-tsdoc` through `pnpm lint`.
- Consumer-visible changes to public `@fluojs/*` package behavior, API surface, package contents, or release metadata require a committed `.changeset/*.md` file. Docs-only, test-only, and internal-only changes usually do not need one.
- Runtime behavior or API changes must keep affected package READMEs, governed docs, and regression tests aligned with [`docs/contracts/behavioral-contract-policy.md`](./contracts/behavioral-contract-policy.md).

## Package Families

| Family | Purpose | Representative packages |
| --- | --- | --- |
| Core | Decorators, DI, config, i18n, runtime orchestration | `@fluojs/core`, `@fluojs/di`, `@fluojs/config`, `@fluojs/i18n`, `@fluojs/runtime` |
| HTTP | Request execution and API surface | `@fluojs/http`, `@fluojs/graphql`, `@fluojs/validation`, `@fluojs/serialization`, `@fluojs/openapi` |
| Auth | Authentication and authorization | `@fluojs/jwt`, `@fluojs/passport` |
| Platform | Runtime adapters | `@fluojs/platform-fastify`, `@fluojs/platform-nodejs`, `@fluojs/platform-express`, `@fluojs/platform-bun`, `@fluojs/platform-deno`, `@fluojs/platform-cloudflare-workers` |
| Realtime | Bidirectional transport | `@fluojs/websockets`, `@fluojs/socket.io` |
| Persistence | Database and cache integration | `@fluojs/prisma`, `@fluojs/drizzle`, `@fluojs/mongoose`, `@fluojs/redis`, `@fluojs/cache-manager` |
| Patterns | Messaging and orchestration patterns | `@fluojs/microservices`, `@fluojs/cqrs`, `@fluojs/event-bus`, `@fluojs/cron`, `@fluojs/queue`, `@fluojs/notifications`, `@fluojs/email`, `@fluojs/slack`, `@fluojs/discord` |
| Operations | Health, metrics, throttling | `@fluojs/metrics`, `@fluojs/terminus`, `@fluojs/throttler` |
| UI | React integration | `@fluojs/react` |
| Tooling | CLI, diagnostics, and Vite build integration | `@fluojs/cli`, `@fluojs/studio`, `@fluojs/testing`, `@fluojs/vite` |

CQRS dispatch context discoverability is split across `packages/cqrs/README.md`, [`docs/architecture/cqrs.md`](./architecture/cqrs.md), and [`docs/reference/package-surface.md`](./reference/package-surface.md): `CqrsDispatchContext` is an opaque, frozen fieldless runtime-agnostic pass-through value whose trusted topology and shutdown-drain state remains private, event-handler and saga fan-out identity follows singleton provider tokens, and application registration stays on the `CqrsModule.forRoot(...)` facade instead of low-level provider assembly helpers. CQRS local handlers complete before sagas and delegated `@fluojs/event-bus` publication, while `publishAll(...)` awaits that full pipeline for each event.

Core request-pipeline metadata seam discoverability is split across `packages/core/README.md` and [`docs/reference/package-surface.md`](./reference/package-surface.md): `@fluojs/core/request-pipeline` is the documented package-integration seam for first-party request-pipeline packages such as `@fluojs/validation`, `@fluojs/serialization`, and `@fluojs/openapi` to share DTO validation, binding, and standard decorator metadata-bag access without importing `@fluojs/core/internal` directly. Application code should continue to use root `@fluojs/core` decorators and public helpers unless a package-integration contract explicitly applies.

Core DI and NestJS migration discoverability is split across `packages/core/README.md`, `packages/di/README.md`, `packages/runtime/README.md`, [`docs/architecture/di-and-modules.md`](./architecture/di-and-modules.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), [`book/beginner/ch04-decorators-intro.md`](../book/beginner/ch04-decorators-intro.md), and [`book/advanced/ch16-custom-package.md`](../book/advanced/ch16-custom-package.md): `@Inject(...)` is a class decorator that declares constructor Tokens in parameter order rather than a property or parameter decorator; circular Module imports are rejected during Module Graph compilation and must be removed by extracting shared Providers into a separate Module or package; and `forwardRef(...)` only delays one dependency Token lookup inside class-level `@Inject(...)` or Provider `inject`, without making Module cycles or true constructor cycles resolvable.

Validation package-surface discoverability is split across `packages/validation/README.md`, [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), [`docs/contracts/nestjs-parity-gaps.md`](./contracts/nestjs-parity-gaps.md), [`docs/reference/package-surface.md`](./reference/package-surface.md), and [`book/beginner/ch06-validation.md`](../book/beginner/ch06-validation.md): `@fluojs/validation` owns standard-decorator input validation, DTO materialization, validation-only checks that may temporarily materialize plain nested values without replacing caller properties, mapped DTO helpers, and the `@fluojs/validation/mapped-types` helper subpath. Ordinary field validators skip `null` and `undefined`, `@IsDefined()` owns requiredness, plain-object materialization retains safe own enumerable extra properties, and class-validator-style `groups` / `always` execution is unsupported. These are fluo-native contracts rather than one-to-one NestJS `ValidationPipe` behavior. Mapped DTO helpers preserve field-level metadata but intentionally do not copy class-level validators onto subset or partial DTOs.
OpenAPI package-surface discoverability is split across `packages/openapi/README.md`, [`docs/architecture/openapi.md`](./architecture/openapi.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), [`docs/contracts/nestjs-parity-gaps.md`](./contracts/nestjs-parity-gaps.md), [`docs/reference/package-surface.md`](./reference/package-surface.md), and [`book/beginner/ch10-openapi.md`](../book/beginner/ch10-openapi.md): `@fluojs/openapi` owns descriptor-driven OpenAPI 3.1.0 document generation, explicit `sources`/`descriptors` registration through `OpenApiModule.forRoot(...)` or `forRootAsync(...)`, later-descriptor path/method collision precedence with explicit descriptors composed after discovered sources, `ui: true` opt-in Swagger UI serving with configurable `swaggerUiAssets`, request decorator overrides for inferred path/query/header/cookie/body documentation, explicit response content metadata rather than handler return-shape inference, default error response policy handling, and `documentTransform` hooks before documents are served.

GraphQL package-surface discoverability is split across `packages/graphql/README.md`, [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and [`book/intermediate/ch18-graphql.md`](../book/intermediate/ch18-graphql.md): `@fluojs/graphql` discovers decorated resolver classes registered as providers or controllers in compiled modules, while the optional `GraphqlModule.forRoot({ resolvers })` list filters that discovery as an allowlist and omission or an empty list permits every registered candidate. It supports root operation resolvers rather than object field resolvers and exposes no `forRootAsync(...)` or `@Subscription({ topics })` contract. TypeScript return types do not define the GraphQL schema: omitted operation output types use GraphQL `String`, object results require `outputType`, and arrays require `outputType: listOf(ItemType)`. The package declares Node.js `>=20.16.0`, covering the effective floor of its mandatory first-party dependency graph because `@fluojs/runtime` transitively depends on `@fluojs/config`, which also requires Node.js `>=20.16.0`; HTTP and SSE use a Web-standard HTTP seam within that supported runtime boundary, while optional WebSocket subscriptions additionally require a server-backed Node HTTP/S adapter with upgrade listeners. Bun, Deno, and Cloudflare Workers are not supported GraphQL package runtimes without aligned dependency metadata and native runtime verification.

React package discoverability is split across `packages/react/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/reference/package-chooser.md`](./reference/package-chooser.md), [`docs/contracts/release-governance.md`](./contracts/release-governance.md), and `examples/react-stable-ssr/README.md`: `@fluojs/react` is currently a runtime-neutral `0.1.0` integration whose root package declares React and React DOM peer ownership for consuming applications and exposes `ReactModule.forRoot(...)`, `ReactModuleOptions`, `@Router(...)`, `@Path(...)`, React metadata readers, `ReactServerEntry`, `createReactServerEntry(...)`, and `renderReactResponse(...)`. `ReactModule.forRoot({ controllers: [...] })` feeds React routers into ordinary fluo module/controller metadata, while the React decorators remain lexical facades over `@fluojs/http` controller and `GET` route metadata, preserving HTTP-owned matching, dispatch, route grammar, DTO binding, versioning, guards, interceptors, headers, module-level middleware, request scope, and duplicate route detection before React entries stream through lazy `react-dom/server` `renderToReadableStream(...)`; the package now accepts explicit hydration asset options for `bootstrapScripts`, `bootstrapModules`, trusted `bootstrapScriptContent`, CSP `nonce`, `identifierPrefix`, and trusted `assetMap` snapshots. The stable root boundary is HTTP-first SSR only: `@fluojs/react/vite` owns parsing already-loaded Vite build manifests into deterministic CSS, JavaScript bootstrap assets, asset maps, trusted bootstrap data, CSP nonce propagation, resolved server/client entries, and diagnostics, while future `@fluojs/react/client` and `@fluojs/react/experimental/rsc` subpaths own browser navigation/client hydration helpers and RSC/server-function experiments. The root still avoids Vite manifest discovery, filesystem scanning, arbitrary inline serialization, React Server Components or Server Functions integration, client bundle generation, Node pipeable stream root APIs, Next.js App Router positioning, TanStack route trees, Angular `Routes[]`, file routes, React-owned `routes: []` tables, or a separate React matcher/router table.

Serialization package-surface discoverability is split across `packages/serialization/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), package-local `packages/serialization/src/serializer-interceptor.test.ts` unit coverage, and runtime request-pipeline coverage in `packages/runtime/src/application.test.ts`: `@fluojs/serialization` owns decorator-aware response serialization, `ExposeClassOptions` class-level `Expose(...)` options such as `excludeExtraneous`, `TransformFunction`, `serialize(value)`, and HTTP `SerializerInterceptor` response-boundary integration. Request-facing interceptor behavior should remain covered through the runtime request pipeline with `bootstrapApplication(...)` and `app.dispatch(...)`, rather than adding a serialization-to-testing workspace dependency edge.

Canonical package and runtime coverage lives in [`docs/reference/package-surface.md`](./reference/package-surface.md), including core additions such as the fluo-native `@fluojs/i18n` root package boundary that depends only on `@fluojs/core`, has no Node.js engine floor, and registers `I18nService` globally by default through `I18nModule.forRoot(...)` with `global: false` as the module-local opt-out, its `@fluojs/i18n/icu` ICU MessageFormat subpath for plural/select localization, `@fluojs/i18n/http` HTTP locale helpers and opt-in `Accept-Language` policy helpers, its `@fluojs/i18n/adapters` subpath for opt-in non-HTTP locale resolution and header policy helpers, `@fluojs/i18n/validation` validation localization, `@fluojs/i18n/loaders/fs` and `@fluojs/i18n/loaders/remote` catalog loaders with opt-in remote cache wrappers, `@fluojs/i18n/typegen` catalog key and typed translation helper declaration generation, `@fluojs/runtime` application-facing helper subpaths `@fluojs/runtime/node` and `@fluojs/runtime/web` plus the `@fluojs/runtime/internal*` package-integration seam boundary, request-pipeline responsibilities such as `@fluojs/serialization` decorator-aware response serialization, output DTO shaping, and HTTP `SerializerInterceptor` response-boundary integration, messaging responsibilities such as `@fluojs/microservices` support for TCP, Redis Pub/Sub, Redis Streams, NATS, Kafka, RabbitMQ, MQTT, and gRPC, `@fluojs/cqrs` command/query buses with bootstrap-time handler discovery, saga orchestration, and event-bus delegation, `@fluojs/event-bus` in-process domain event fan-out with optional Redis Pub/Sub transport, inherited event channel fan-out, bounded publish cancellation/timeouts, and shutdown drain semantics for both local publishes and inbound transport callbacks, and persistence responsibilities such as `@fluojs/mongoose` ALS/session transaction ownership. Task-based package discovery lives in [`docs/reference/package-chooser.md`](./reference/package-chooser.md), including localization/i18n selection guidance for `@fluojs/i18n`, its ICU, HTTP locale policy, adapters header policy, validation, loader/cache, and typegen subpaths, response serialization and output DTO shaping selection guidance for `@fluojs/serialization`, optional cross-process `@fluojs/event-bus` fan-out selection with `@fluojs/event-bus/redis` and `@fluojs/redis`, plus operations guidance for `@fluojs/terminus`, its `@fluojs/terminus/node` Node memory/disk indicator subpath, its `@fluojs/terminus/redis` Redis indicator subpath, and `execution.indicatorTimeoutMs` slow-indicator timeout guardrails. Ecosystem bridge compatibility decisions for NestJS i18n, i18next, next-intl, and request/validation convenience glue live in [`docs/reference/i18n-ecosystem-bridges.md`](./reference/i18n-ecosystem-bridges.md).

NestJS i18n migration discoverability is split across `packages/i18n/README.md`, [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and [`docs/reference/i18n-ecosystem-bridges.md`](./reference/i18n-ecosystem-bridges.md): register catalogs through `I18nModule.forRoot(...)`, run an ordered `resolveHttpLocale(...)` resolver chain at an application-owned request boundary, read the selected locale from that `RequestContext` with `getHttpLocale(...)`, and pass it explicitly to translation or `localizeDtoValidationError(...)`. The validation helper remains transport-agnostic and never reads request or global locale state implicitly.

Microservices discoverability is split across `packages/microservices/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), the [RabbitMQ](../book/intermediate/ch04-rabbitmq.md), [Kafka](../book/intermediate/ch05-kafka.md), and [NATS](../book/intermediate/ch06-nats.md) transport chapters, root/subpath export tests, and transport regression suites: application registration stays on `MicroservicesModule.forRoot(...)`, `createMicroservicesProviders(...)` remains a compatibility/manual provider-array helper, and `MICROSERVICE` exposes the programmatic `Microservice` facade rather than a raw transport instance. `send()` settles on a correlated remote response, while `emit()` settles when outbound transport publication completes rather than when remote handlers finish; Kafka and RabbitMQ separately keep inbound consumer callbacks pending through handler execution and response publication. `Microservice.close(signal?: string)` forwards shutdown signals to `MicroserviceLifecycleService.close(signal)` while configured transports keep `close(): Promise<void>` unless a transport documents signal-aware shutdown, and caller-owned NATS, Kafka, and RabbitMQ collaborators must be closed or drained by the application after facade shutdown. Transport-specific subpaths expose TCP, Redis Pub/Sub, NATS, Kafka, RabbitMQ, MQTT, and gRPC while `RedisStreamsMicroserviceTransport` remains root-barrel-only, root TCP imports keep `node:net` lazy until listen or outbound socket construction paths, request/reply and event payloads are cloned before handler dispatch, TCP `port: 0` routes outbound calls through the OS-assigned listener, internally-created gRPC servers are shut down by the transport, internally-created MQTT clients are closed when listen setup fails or close unwinds a failed in-flight listen attempt, and direct `@fluojs/core/internal` imports are treated as a first-party package-integration seam for metadata/clone helpers rather than an application-facing surface.

Microservices handler migration discoverability is mirrored in the [microservices introduction](../book/intermediate/ch01-microservices-intro.md): `@MessagePattern` and `@EventPattern` use the TC39 standard method-decorator context without NestJS provider metadata, `reflect-metadata`, `experimentalDecorators`, or `emitDecoratorMetadata`; owning classes must be registered explicitly in compiled module `providers` or `controllers`; and discovery invokes decorated public instance methods rather than private or static targets.

Event-bus package-surface discoverability is split across `packages/event-bus/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/reference/package-chooser.md`](./reference/package-chooser.md), and [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md): `EventBusModule.forRoot({ global? })` keeps event-bus providers global by default with `global: false` as the module-local opt-out, the optional `@fluojs/event-bus/redis` subpath owns Redis Pub/Sub fan-out, directly declared `static eventKey` values stabilize distributed routing, and shutdown drains awaited plus `waitForHandlers: false` background handler/transport work before transport close or the configured drain timeout. Regression coverage must keep per-call publish timeouts, shared explicit `eventKey` subscription dedupe, background and inbound shutdown drain paths, and app teardown executable.

Config runtime-boundary discoverability is split across `packages/config/README.md`, governed package-surface docs, and the package folder-structure rules: the root `@fluojs/config` import remains safe for `ConfigService` and explicit in-memory `loadConfig(...)` consumers because Node filesystem/path/crypto builtins are resolved lazily only when env-file loading or watch mode executes; env-file, default `.env`, and watch paths require `process.getBuiltinModule(...)`, use a `node:module` fallback when direct filesystem/path/crypto lookup is unavailable, and are supported under the package-level Node.js 20.16.0-or-newer engine range. [`docs/reference/package-surface.md`](./reference/package-surface.md) records that root-import contract and [`docs/reference/package-folder-structure.md`](./reference/package-folder-structure.md) records the root-entrypoint constraint for runtime-specific Node builtins.

Vite tooling discoverability is split across `packages/vite/README.md`, [`docs/reference/package-chooser.md`](./reference/package-chooser.md), and [`docs/reference/toolchain-contract-matrix.md`](./reference/toolchain-contract-matrix.md): use `@fluojs/vite` when a Node.js `>=20.0.0` Vite project must build TypeScript with TC39 standard decorators through Babel's `@babel/plugin-proposal-decorators` `{ version: '2023-11' }` transform plus `@babel/preset-typescript` while preserving fluo's Vite file-boundary skips. Generated non-Deno starters keep `vite.config.ts` on `@fluojs/vite` for application files and `vitest.config.ts` on `@fluojs/testing/vitest` for test files, so Vite `>=6.2.0` and lazy Babel peer diagnostics for `@babel/core`, `@babel/plugin-proposal-decorators`, and `@babel/preset-typescript` stay on the application transform boundary while package import, plugin creation, and test transforms stay outside that lazy-load boundary.

Testing package discoverability is split across `packages/testing/README.md`, [`docs/contracts/testing-guide.md`](./contracts/testing-guide.md), [`docs/reference/package-surface.md`](./reference/package-surface.md), and [`book/beginner/ch20-testing.md`](../book/beginner/ch20-testing.md): `@fluojs/testing` is a Node.js 20+ package whose app helpers require explicit `rootModule` graphs, request-facing guard/interceptor overrides should be covered through dispatch or `createTestApp(...)`, `createTestApp()` cleanup belongs in `finally`, request-level helpers preserve isolated request-scoped DI through both `app.request(...).send()` and `TestingModuleRef.dispatch(...)`, `@fluojs/testing/http` is the documented request helper subpath, portability harnesses preserve setup/assertion cleanup failures, and `DeepMocked<T>` remains on the documented Vitest-compatible root/types/mock boundary while non-mock helpers stay on app/module/harness entrypoints.

HTTP runtime discoverability is split across `packages/http/README.md`, [`docs/architecture/http-runtime.md`](./architecture/http-runtime.md), NestJS migration docs, and the intermediate Express/Node book chapter: the package README documents DTO field-level binding decorators, lazy `RequestContext` storage resolution that preserves synchronous callback return/throw behavior while isolating promise continuations, managed SSE `AsyncIterable` support, and dispatcher preservation of both `FrameworkRequest.signal` and `FrameworkRequest.isAborted()` cancellation surfaces; [`docs/architecture/http-runtime.md`](./architecture/http-runtime.md) carries the canonical request lifecycle and abort check ordering; `packages/platform-express/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/reference/package-chooser.md`](./reference/package-chooser.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and [`book/intermediate/ch21-express-node.md`](../book/intermediate/ch21-express-node.md) carry the Express host compatibility boundary: Express remains the platform engine, application middleware remains fluo `Middleware`, native Express/Connect `(req, res, next)` functions are not portable fluo middleware, `getServer()` / `getListenTarget()` / `getRealtimeCapability()` stay infrastructure-boundary helpers, and duplicate `listen()` calls on a started adapter are idempotent while preserving the live dispatcher. `packages/platform-nodejs/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), and [`book/intermediate/ch21-express-node.md`](../book/intermediate/ch21-express-node.md) carry the raw Node.js adapter boundary: `@fluojs/platform-nodejs` is a Node.js 20+ package with `engines.node >=20.0.0`, package-local `Nodejs*` type aliases, direct Node `http`/`https` listener ownership, retry/body-limit/shutdown contracts, `x-correlation-id` error fallback coverage, explicit `multipart.maxTotalSize` override coverage, and non-racy `port: 0` signal-shutdown regression coverage. The same migration/book surfaces carry the SSE distinction between `SseResponse`, `AsyncIterable`, and unsupported Observable return conversion, plus the DTO binding boundary that migrates NestJS Pipe/`ValidationPipe` assumptions to `@RequestDto(...)`, field-level `@From*` decorators, `@Convert(...)`, and post-materialization validation.

The Express native middleware seam is the platform-specific `nativeMiddleware` adapter option. It mounts Express/Connect request and error handlers in array order before Express Router and fluo dispatch; `next()` continues into fluo, a native response terminates before dispatch, native errors stay in the Express error chain, and resources captured by native handlers remain application-owned.

HTTP learning-path alignment is carried by [`book/advanced/ch11-request-pipeline.md`](../book/advanced/ch11-request-pipeline.md), [`book/advanced/ch13-custom-adapter.md`](../book/advanced/ch13-custom-adapter.md), [`book/beginner/ch05-routing-controllers.md`](../book/beginner/ch05-routing-controllers.md), and [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md): request context is async only when the host supplies an async-context primitive and otherwise uses a synchronous-only fallback; dispatch abort checks are documented boundaries rather than automatic cancellation of arbitrary application work; the dispatcher disposes promoted request scopes but not arbitrary app-owned files or streams; custom adapter examples must satisfy the complete `FrameworkRequest` / `FrameworkResponse` shape and use `setStatus(...)`; route hierarchy is expressed directly in route paths rather than through nested Controllers; and NestJS controller parameter decorators migrate to DTO field binding plus an explicit `RequestContext` parameter when low-level access is necessary.

Fastify adapter discoverability is split across `packages/platform-fastify/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/reference/package-chooser.md`](./reference/package-chooser.md), and the beginner prerequisite/bootstrap/production chapters. `@fluojs/platform-fastify` is the default high-performance Node.js 20+ adapter path, declares `engines.node >=20.0.0`, owns a Fastify-backed Node `http`/`https` listener, and accepts Node.js `https` server options through `createFastifyAdapter(...)`, `bootstrapFastifyApplication(...)`, and `runFastifyApplication(...)` when the process terminates TLS directly. Beginner docs keep local starter execution on plain HTTP while directing production TLS either to infrastructure termination or to the explicit adapter `https` option with certificate material loaded at the application configuration or secret-management boundary.

Realtime Socket.IO discoverability is split across `packages/socket.io/README.md`, [`docs/reference/package-chooser.md`](./reference/package-chooser.md), NestJS migration docs, and the intermediate Socket.IO book chapter: the package README documents `SocketIoModule.forRoot(...)`, the runtime-neutral `SocketIoHandshakeRequest` root type, guard acceptance for `true` / `undefined` / no return and rejection for `false` / `SocketIoGuardRejection`, deny-by-default CORS, positive-integer numeric option validation, Node.js 20+ server-backed and official Bun engine runtime limits, Bun static CORS plus no-`serverBacked` gateway caveats, unsupported Deno/Workers status, adapter-owned/shared HTTP listener ownership during shutdown, bounded shutdown force-disconnect/retry semantics, explicit ACK callback handling because Socket.IO gateway return values are ignored, raw `SOCKETIO_SERVER` migration seams for native multi-room/volatile emits, and the `@fluojs/websockets` companion decorator requirement; [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the canonical `@fluojs/socket.io` runtime limitation summary.

Realtime WebSockets discoverability is split across `packages/websockets/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), and [`docs/reference/package-chooser.md`](./reference/package-chooser.md): the package README documents the root `@fluojs/websockets` Node.js default, root import behavior that preserves `WebSocketModule` and the token-only `WebSocketGatewayLifecycleService` export name while lazily resolving the concrete Node `ws` lifecycle implementation during runtime provider resolution, runtime-specific subpaths `@fluojs/websockets/node`, `@fluojs/websockets/bun`, `@fluojs/websockets/deno`, and `@fluojs/websockets/cloudflare-workers`, the shared gateway decorator and metadata authoring primitives exposed from each subpath so fetch-style applications can author gateways without relying on the root Node.js-backed module boundary, room management through `WebSocketRoomService`, normalized text/binary payload dispatch, typed runtime seams for upgrade context/guard/rejection and gateway descriptors, runtime socket/binding types on each subpath, Node `IncomingMessage` versus fetch-style `Request` upgrade guard typing, rejected pre-upgrade guard outcomes through booleans, structured rejection objects, or thrown HTTP exceptions, ignored raw WebSocket handler return values with explicit socket-send replies, and bounded shutdown behavior for unresolved disconnect cleanup.

Redis integration discoverability is split by responsibility: `packages/redis/README.md` documents synchronous `RedisModule.forRoot(...)` registration, named client tokens through `getRedisClientToken(name)`, `RedisModule.forRoot({ lifecycle })` connect/quit timeout guardrails, platform health/readiness status snapshot helper responsibility, and the raw-client rule that Pub/Sub subscribers need dedicated Redis connections. Each `RedisModule.forRoot(...)` registration creates a new client from final options and does not adopt an external client; application-owned raw publisher and duplicate subscriber clients must be closed by the application. [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the canonical `@fluojs/redis` surface summary; [`book/intermediate/ch03-redis-transport.md`](../book/intermediate/ch03-redis-transport.md) explains the Redis Pub/Sub versus Redis Streams learning path, including why shared command clients should not be reused as subscribed Pub/Sub connections and how named subscriber registrations let fluo own subscriber lifecycle timeouts; and [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records that NestJS Redis async-module assumptions must move to the application boundary before passing final options into fluo's synchronous Redis module registration.

Observability discoverability is split across metrics and health packages: `packages/metrics/README.md`, [`docs/architecture/observability.md`](./architecture/observability.md), and [`book/beginner/ch19-metrics.md`](../book/beginner/ch19-metrics.md) document the Node.js 20+ `@fluojs/metrics` boundary, `MetricsModule.forRoot(...)`, isolated/shared registry ownership, `MetricsService.getRegistry()`, the re-exported `Registry` type, low-level `METER_PROVIDER` / `PrometheusMeterProvider`, meter abstraction types such as `MeterProvider`, `MeterCounter`, `MeterGauge`, and `MeterHistogram`, opt-in HTTP request collectors with shared-registry path-label compatibility checks, endpoint-scoped `endpointMiddleware`, distinct module-level `middleware`, active-registry scrape refresh through both the built-in `/metrics` controller and advanced `MetricsService.getRegistry().metrics()` callers, and registry-scoped platform telemetry stale-series cleanup; [`docs/contracts/deployment.md`](./contracts/deployment.md) records deployment expectations for exposing metrics routes safely, and `examples/ops-metrics-terminus/README.md` shows the current health/readiness/metrics project structure; `packages/terminus/README.md` documents `TerminusModule.forRoot(...)`, indicator/provider registration, exported indicator/provider DI tokens, execution timeouts, service-scoped in-flight indicator serialization, Prisma/Drizzle lifecycle-aware diagnostics, and Redis lifecycle-aware diagnostics through `@fluojs/terminus/redis`; and [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the canonical responsibility boundary for both packages, including the metrics package manifest `engines.node >=20.0.0` floor.

Terminus runtime-boundary discoverability is split across the package README, beginner book, and governed package-surface docs: `packages/terminus/README.md` documents root-safe `TerminusModule.forRoot(...)` registration, `indicatorProviders`, repeatable provider factories, exported indicator/provider DI tokens, Prisma named service/client provider seams and Drizzle provider seams that preserve root optional-peer safety, service-scoped in-flight indicator serialization, Redis/Drizzle lifecycle-aware diagnostics, Node-specific `@fluojs/terminus/node` indicator helpers, and lazy filesystem access for disk checks; [`book/beginner/ch18-health.md`](../book/beginner/ch18-health.md) carries the learning-path summary for optional-peer-safe Prisma/Drizzle/Redis health probes; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/terminus` responsibility boundary for health/readiness aggregation, optional-peer subpaths, and runtime-safe indicator composition.

Cache-manager discoverability is split across the package, governed package-surface docs, the cache contract, and the beginner book: `packages/cache-manager/README.md` documents synchronous `CacheModule.forRoot(options)`, store selection, `CacheService`, cache decorators, low-level metadata helper exports, the compatibility-only `NormalizedCacheModuleOptions` type export, and platform status/diagnostic helpers; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/cache-manager` responsibility boundary; [`docs/architecture/caching.md`](./architecture/caching.md) carries current cache behavior contracts; and [`book/beginner/ch17-cache.md`](../book/beginner/ch17-cache.md) teaches the application-facing configuration flow.

Throttler discoverability is split across the package README and governed package-surface docs: `packages/throttler/README.md` documents `ThrottlerModule.forRoot(options)`, explicit `ThrottlerGuard` activation, route/class throttling decorators, memory and Redis/custom store contracts, proxy-aware client identity controls, shared route/client bucket semantics, custom-store `retryAfterMs` support for authoritative backing-store clocks, status input/output types, and platform status/diagnostic helpers; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/throttler` responsibility boundary for request rate limiting, backing-store readiness, ownership, and local or distributed operation visibility.

Queue lifecycle and migration discoverability is split across the package and governed docs: `packages/queue/README.md` documents the package manifest's Node.js `>=20.0.0` runtime floor, `QueueModule.forRoot(...)`, the `QueueModuleOptions.global` module-scope control including `global: false` worker/Redis reachability, explicit singleton `@QueueWorker(JobClass)` migration from NestJS/Bull processor metadata, the bootstrap-ready worker processor handoff, bounded shutdown through `workerShutdownTimeoutMs`, dead-letter access, and lifecycle status snapshots; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records that NestJS Bull/BullMQ may persist multiple named job values under one `queueName`, while fluo uses `jobName` as both queue name and named job, so `jobName` alone cannot preserve that topology or make a persisted payload and metadata compatible; [`book/intermediate/ch11-queue.md`](../book/intermediate/ch11-queue.md) teaches the required drain, transform-and-re-enqueue, or separate-queue cutover and lifecycle checklist; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical Queue responsibility and runtime boundary; [`docs/contracts/behavioral-contract-policy.md`](./contracts/behavioral-contract-policy.md) treats readiness and shutdown ordering as behavioral contracts that require implementation, docs, and regression tests to change together; and [`docs/contracts/testing-guide.md`](./contracts/testing-guide.md) identifies the nearest package tests and governance commands to run when queue lifecycle behavior changes.

Notifications discoverability is split across the package README, governed package-surface docs, NestJS migration docs, and the intermediate book: `packages/notifications/README.md` documents `NotificationsModule.forRoot(...)` / `forRootAsync({ inject, useFactory, global? })`, explicit `NotificationChannel` values passed through `channels`, global-by-default provider visibility with `global: false` local opt-out, direct `dispatch(...)`, batch `dispatchMany(...)`, optional queue-backed delivery where `queue: true` explicitly forces queue delivery regardless of `bulkThreshold`, lifecycle event publication, platform status snapshots through `NotificationsService.createPlatformStatusSnapshot()` and `createNotificationsPlatformStatusSnapshot(...)` whose operation mode, dependencies, queue threshold, queue configuration, and event-publisher configuration live under `details`, deterministic generated queue/fallback delivery IDs that remain a best-effort fallback for plain envelopes rather than a recursive opaque-payload contract, and intentional limitations around concrete queue/event-bus ownership; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/notifications` responsibility boundary, including that concrete queue and event-bus implementations remain outside the foundation package and caller-provided `notification.id` is the durable identity contract when cross-release delivery identity matters; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records the migration boundary from NestJS provider discovery/decorator metadata to explicit channel registration, application-owned queue/event publisher resources, and global-by-default service/token visibility; and [`book/intermediate/ch15-notifications.md`](../book/intermediate/ch15-notifications.md) teaches the same registration, batch/status, queue, lifecycle, and diagnostics surfaces in the FluoShop learning path.

Cron scheduling discoverability is split across the package, governed package-surface docs, package chooser, lifecycle docs, NestJS migration docs, and the intermediate book: `packages/cron/README.md` documents `CronModule.forRoot(...)`, five-field and six-field cron expression support, public instance method decorator targets, descriptor-based `SCHEDULING_REGISTRY` controls including immutable descriptor snapshots, dynamic blank-name rejection, rollback-safe cron expression and interval cadence updates, decorator and dynamic registry startup behavior, Redis distributed locking, bounded shutdown, public scheduling/status types, and status snapshots, including that Redis peer loading and resolution are distributed-only and occur only when `distributed.enabled` is `true`; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the `@fluojs/cron` responsibility boundary for scheduling, rollback-safe runtime cadence changes, distributed locks, shutdown, health/readiness visibility, enabled distributed TTL validation before Redis I/O, and the same distributed-only Redis peer boundary; [`docs/reference/package-chooser.md`](./reference/package-chooser.md) directs users to add `@fluojs/redis` only when enabling Cron distributed locks; [`docs/architecture/lifecycle-and-shutdown.md`](./architecture/lifecycle-and-shutdown.md) records Cron scheduler ownership, active-task drain, and distributed lock release/shutdown semantics, including shutdown-time Redis owned-lock release I/O bounded by `shutdown.timeoutMs` and local ownership retention when release I/O times out; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records the `@nestjs/schedule` migration boundary from `SchedulerRegistry`/`CronJob` handles to public-method decorators and read-only descriptors; and [`book/intermediate/ch12-cron.md`](../book/intermediate/ch12-cron.md) explains the FluoShop learning path with the same bootstrap, dynamic-start lifecycle guarantees, public-method scheduling boundary, descriptor-based registry controls, rollback-safe interval cadence updates, enabled-TTL validation, bounded lock release, and Redis distributed-lock selection boundary.

Persistence transaction discoverability is split across package READMEs, governed transaction architecture docs, package-surface docs, package chooser docs, NestJS migration docs, and the intermediate book: `packages/prisma/README.md`, `packages/drizzle/README.md`, and `packages/mongoose/README.md` document package-local standard `@Transaction()` decorators as the primary Service-layer transaction boundary, while `transaction(...)`, `requestTransaction(...)`, `current()`, and `currentSession()` remain explicit low-level escape hatches. `packages/drizzle/README.md` also records the root `@fluojs/drizzle` Node.js 20+ runtime boundary (`node:async_hooks`, `engines.node >=20.0.0`), raw Drizzle provider guidance for Bun, Deno, Cloudflare Workers, and other non-Node drivers, fail-open direct execution when `database.transaction(...)` is unavailable and `strictTransactions` is `false`, production `strictTransactions: true` guidance for rollback guarantees, host-object heuristic target selection (`this.db`, direct properties, nested `.db`), explicit accessor selection such as `@Transaction((self) => self.ordersDb)` for multiple Drizzle clients, controller/request-wide `requestTransaction(...)` compatibility guidance, application registration on `DrizzleModule.forRoot(...)` / `forRootAsync(...)`, and `DrizzleDatabase.createFacade(...)` as a compatibility-only helper. [`docs/architecture/transactions.md`](./architecture/transactions.md) records the cross-package boundary semantics, restored deprecated `PrismaTransactionInterceptor` and `MongooseTransactionInterceptor` compatibility exports while `DrizzleTransactionInterceptor` remains unavailable, the explicit `requestTransaction(...)` path for rare request-wide boundaries, Drizzle fail-open versus strict transaction behavior, Drizzle decorator target selection, and the Mongoose-supported auto-session facade method set; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) and [`book/intermediate/ch20-drizzle.md`](../book/intermediate/ch20-drizzle.md) carry migration and learning-path guidance for those Drizzle transaction choices; [`docs/reference/package-surface.md`](./reference/package-surface.md) and [`docs/reference/package-chooser.md`](./reference/package-chooser.md) carry the canonical Drizzle Node-only selection, non-Node raw-provider fallback, and public API naming summary.

Drizzle transaction drain discoverability specifically lives in `packages/drizzle/README.md`, [`docs/architecture/transactions.md`](./architecture/transactions.md), and [`book/intermediate/ch20-drizzle.md`](../book/intermediate/ch20-drizzle.md): fail-open manual `transaction(...)` callbacks are tracked during shutdown and must settle before `dispose(database)`, while controller-level `@Transaction()` is documented only as a compatibility path and explicit `requestTransaction(...)` remains the clearer request-wide boundary.

Drizzle transaction target selection falls back to the decorated instance itself after `this.db`, direct host properties, and nested `.db` candidates fail to expose `transaction(...)`; services with multiple possible Drizzle clients should keep using an explicit accessor such as `@Transaction((self) => self.ordersDb)`.

Prisma transaction boundary discoverability is specifically carried by `packages/prisma/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and the beginner Prisma, transaction, and production chapters: `@fluojs/prisma` is the Node.js 20+ Prisma integration path backed by host `AsyncLocalStorage`; service `@Transaction()` is the primary business boundary; explicit request-wide `requestTransaction(...)` calls should forward `RequestContext.request.signal`; `PrismaService<TClient>` owns lifecycle/transaction wrapper methods while `PrismaServiceFacade<TClient>` forwards generated delegates; default `@Transaction()` resolution selects only branded Prisma service/facade hosts and ambiguous hosts should pass explicit accessors; unsupported `$transaction(...)` falls back without rollback atomicity unless `strictTransactions` is enabled; unavailable or throwing ALS host lookups fail closed with `transactionContext: 'unavailable'`; shutdown drains active request plus service/manual transaction boundaries before `$disconnect()`; and automatic primary/read-replica routing, built-in transaction metrics, and ALS-provided public transaction IDs are not package contracts.

Mongoose integration discoverability is split across the package and governed package-surface docs: `packages/mongoose/README.md` documents the root `@fluojs/mongoose` Node.js 20+ runtime boundary (`node:async_hooks`, `engines.node >=20.0.0`), `MongooseModule.forRoot(...)` / `forRootAsync(...)`, application-owned concrete connection requirements and `dispose(connection)` ownership, exported option and status input types such as `MongooseAsyncModuleOptions<TConnection>` and `MongoosePlatformStatusSnapshotInput`, lifecycle/session behavior, service `@Transaction()` session facade behavior, `MongooseConnection.model(...)` auto-session support for `create`, `find`, `findOne`, `aggregate`, and `bulkWrite`, unsupported `doc.save()` / raw model-method escape hatches through `currentSession()`, delegated `connection.transaction(...)` semantics, fail-open direct execution when transaction APIs are unavailable unless `strictTransactions` is enabled, fail-open manual `transaction(...)` callback drain before `dispose(connection)`, `MongooseConnection.createPlatformStatusSnapshot()` / `createMongoosePlatformStatusSnapshot(...)` diagnostics, and the compatibility-only role of `createMongooseProviders(...)`; [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the namespace-facade policy that keeps application registration on `MongooseModule.forRoot(...)` / `forRootAsync(...)` while reserving provider helpers for manual composition compatibility and records the same Node-only root-wrapper boundary; [`docs/reference/package-chooser.md`](./reference/package-chooser.md) directs non-Node runtimes to application-owned raw Mongoose-compatible providers until a runtime-specific transaction-context adapter is documented; and [`docs/contracts/behavioral-contract-policy.md`](./contracts/behavioral-contract-policy.md) governs transaction lifecycle, readiness, and shutdown documentation parity.

JWT auth discoverability is split across the package README, governed package-surface docs, and the beginner authentication chapter: `packages/jwt/README.md` documents `JwtModule`, signing and verification providers, refresh-token storage/rotation contracts, optional `RefreshTokenStore.revokeByFamily(...)` reuse handling with a compatible `revokeBySubject(...)` fallback, root-import safety, and status/diagnostic exports such as `JwtPlatformStatusSnapshot`, `JwtStatusAdapterInput`, `createJwtPlatformStatusSnapshot(...)`, and `createJwtPlatformDiagnosticIssues(...)`; [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the canonical `@fluojs/jwt` package responsibility summary; and [`book/beginner/ch14-jwt.md`](../book/beginner/ch14-jwt.md) teaches the refresh-token flow, including durable `rotate(...)`, consume-only compatibility, family-scoped replay response, and the requirement that `RefreshTokenService.issueRefreshToken(...)` and `RefreshTokenService.rotateRefreshToken(...)` receive `JwtModule` `refreshToken` options plus a durable `RefreshTokenStore` before the storage-backed lifecycle is complete.

Passport auth discoverability is split across the package and governed package-surface docs: `packages/passport/README.md` documents `PassportModule.forRoot(...)` strategy registry wiring, `AuthGuard` and optional auth/scope decorators, Passport.js strategy bridge provider bundles, cookie-auth and refresh-token presets, account-linking helpers, public auth metadata helpers, and platform status/diagnostic helpers such as `createPassportPlatformStatusSnapshot(...)`; [`docs/reference/package-surface.md`](./reference/package-surface.md) carries the canonical `@fluojs/passport` package responsibility summary and namespace-facade exception for `createPassportJsStrategyBridge(...)` plus the compatibility-only `createCookieAuthPreset(...)` provider bundle; and [`docs/contracts/public-export-tsdoc-baseline.md`](./contracts/public-export-tsdoc-baseline.md) keeps public helper documentation aligned with the exported API surface.

Email discoverability is split across the package README, governed package-surface docs, NestJS migration docs, and the intermediate book: `packages/email/README.md` documents `EmailModule.forRoot(...)` / `forRootAsync({ inject, useFactory, global? })`, global-by-default provider visibility for `EmailService`, `EmailChannel`, `EMAIL`, and `EMAIL_CHANNEL`, the explicit `global: false` local-visibility opt-out, the unsupported NestJS `imports` / `useClass` / `useExisting` async-registration shapes, direct `EmailService` delivery, `@fluojs/notifications` channel integration, queue-backed worker registration, lifecycle-gated sends through `EmailLifecycleError`, caller-owned transport shutdown boundaries, notification email field forwarding, and platform status snapshots whose queue metadata is present only when callers provide queue details explicitly; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/email` responsibility boundary; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records the migration boundary from NestJS dynamic-module async shapes to fluo's injected factory; and [`book/intermediate/ch16-email.md`](../book/intermediate/ch16-email.md) teaches the same registration and visibility rules in the FluoShop learning path.

Slack discoverability is split across the package README, governed package-surface docs, NestJS migration docs, and the intermediate chat book chapter: `packages/slack/README.md` documents `SlackModule.forRoot(...)` / `forRootAsync(...)`, injected-factory-only async registration through `inject` and `useFactory` after application-graph dependency registration, default global provider visibility with `global: false` local opt-out, singleton compatibility tokens `SLACK` and `SLACK_CHANNEL`, manual singleton provider composition through `createSlackProviders(...)`, app-owned composition for multiple Slack clients, direct `SlackService` delivery, `@fluojs/notifications` channel integration, abort-signal propagation, lifecycle-gated sends, `verifyOnModuleInit` bootstrap verification for transports that expose optional `verify()`, serialized factory-owned transport cleanup across bootstrap failure and shutdown, `SlackTemplateRenderer` template rendering with payload-over-rendered merge precedence, and platform status snapshots; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/slack` responsibility boundary for webhook-first, singleton, transport-agnostic Slack delivery without direct `process.env` reads; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records the migration boundary from NestJS `imports` / `useClass` / `useExisting`, multi-client assumptions, and `isGlobal` to Slack's injected factory, app-graph dependency registration, singleton tokens, and `global?: boolean`; and [`book/intermediate/ch17-slack-discord.md`](../book/intermediate/ch17-slack-discord.md) teaches the same module visibility, async registration, singleton token, bootstrap verification, and template-backed notification dispatch surfaces in the FluoShop learning path.

Discord discoverability is split across the package README, governed package-surface docs, NestJS migration docs, and the intermediate chat book chapter: `packages/discord/README.md` documents `DiscordModule.forRoot(...)` / `forRootAsync({ inject, useFactory, global? })`, default global provider visibility with `global: false` local opt-out, singleton compatibility tokens `DISCORD` and `DISCORD_CHANNEL`, intentionally private internal provider helpers/tokens, direct `DiscordService` delivery, `@fluojs/notifications` channel integration through `DISCORD_CHANNEL`, lifecycle-gated sends, factory-owned versus app-owned transport resource ownership, `verifyOnModuleInit` bootstrap verification for transports that expose optional `verify()`, `DiscordTemplateRenderer` notification template rendering, webhook-first transport boundaries, and status snapshots through both `DiscordService.createPlatformStatusSnapshot()` and `createDiscordPlatformStatusSnapshot(...)`; [`docs/reference/package-surface.md`](./reference/package-surface.md) records the canonical `@fluojs/discord` responsibility boundary for webhook-first, transport-agnostic Discord delivery without direct `process.env` reads; [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md) records the migration boundary from NestJS async module/custom-provider assumptions and `isGlobal` to Discord's injected factory plus `global?: boolean`; and [`book/intermediate/ch17-slack-discord.md`](../book/intermediate/ch17-slack-discord.md) teaches standalone `DiscordService`, notifications channel routing, embed payloads, Discord module visibility, and Discord status snapshot observability in the FluoShop learning path.

HTTP adapter raw-body portability discoverability is split across the testing package and governed platform docs: `packages/testing/README.md` documents `createHttpAdapterPortabilityHarness(...)` and `assertPreservesExactRawBodyBytesForByteSensitivePayloads()` for byte-sensitive payloads; [`docs/contracts/platform-conformance-authoring-checklist.md`](./contracts/platform-conformance-authoring-checklist.md) requires HTTP adapters to preserve exact `rawBody` bytes without Unicode replacement, newline normalization, or re-encoding; and [`docs/contracts/testing-guide.md`](./contracts/testing-guide.md) identifies platform portability tests and governance commands to run when HTTP adapter byte preservation behavior changes.

Bun adapter lifecycle and migration discoverability is split across `packages/platform-bun/README.md`, [`book/intermediate/ch22-bun.md`](../book/intermediate/ch22-bun.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and the website runtime/realtime guides: the package README documents duplicate `listen()` idempotency, pre-listen realtime binding requirements, byte-exact raw-body and stale native handoff regression coverage, signal-driven shutdown reporting through logs/`process.exitCode`, and synchronous `createBunFetchHandler(...)` usage; the intermediate book carries the manual fetch-host lifecycle boundary and `runBunApplication(...)` signal helper boundary; NestJS migration docs record that Bun fetch-style hosting does not restore late host mutation or reflection metadata assumptions; and the website guides keep `createBunFetchHandler(...)` synchronous while keeping adapter-owned shutdown, raw fetch, and `server.upgrade(...)` control outside the realtime binding seam.

Deno adapter lifecycle discoverability is split across `packages/platform-deno/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`book/intermediate/ch23-deno.md`](../book/intermediate/ch23-deno.md), and the website runtime adapter guide: managed `listen()` and `runDenoApplication(...)` own `Deno.serve(...)`, bounded shutdown, signals, and configured websocket upgrades, while synchronous `createDenoFetchHandler(...)` accepts an already bootstrapped `app.dispatcher`, preserves shared Web request parsing and SSE framing, never starts a server, and leaves shutdown, signals, and websocket upgrades to the surrounding host.

Cloudflare Workers adapter lifecycle and migration discoverability is split across `packages/platform-cloudflare-workers/README.md`, [`docs/reference/package-surface.md`](./reference/package-surface.md), [`book/intermediate/ch24-cloudflare.md`](../book/intermediate/ch24-cloudflare.md), [`docs/getting-started/migrate-from-nestjs.md`](./getting-started/migrate-from-nestjs.md), and the website runtime/realtime guides: these surfaces align the socketless `listen()` dispatcher binding, Worker `fetch(request, env, ctx)` plus `executionContext.waitUntil(...)` request/SSE/WebSocket tracking, application-owned `env` mapping, complete `CloudflareWorkersWebSocketModule` registration before the frozen first-listen binding, bounded `close()` drain behavior, JSON `503` shutdown responses for new HTTP and WebSocket ingress while draining or closed, and lazy entrypoint recovery after a timed-out close eventually settles.

Release lane discoverability is governed by [`docs/contracts/release-governance.md`](./contracts/release-governance.md): stable patch, minor, and major releases flow through `main`, `tooling/release/verify-changeset-release-lane.mjs` validates stable Changesets metadata in PR CI and release automation, canonical `pnpm verify:release-readiness` runs each split Vitest project with `--maxWorkers=1`, README-documented public CLI command/flag/starter/inspect/programmatic additions require `minor` metadata instead of patch, and major changesets require explicit maintainer approval plus consumer-facing migration notes.

CLI and Studio diagnostics discoverability is split across the CLI package, Studio package, and governed tooling docs: `packages/cli/README.md` documents the `@fluojs/cli` Node.js `>=20.0.0` process runtime floor, the public `NewCommandRuntimeOptions` boundary that excludes monorepo-local starter dependency overrides, `fluo inspect` default JSON output, `--timing` snapshot-plus-timing envelopes, `--report` support artifacts, `--mermaid` Studio delegation, and `--output <path>` artifact writes; `packages/studio/README.md` documents the CLI-owned `fluo dev --studio` sidecar/viewer workflow, the `@fluojs/studio` Node.js `>=20.0.0` tooling package floor, the Node dev-runner live MVP, static/report fallback guidance for Bun, Deno, and Cloudflare Workers, development-only versus runtime dependency installation expectations, Node package-resolution for the packaged viewer fallback, live request event privacy validation that rejects body-like payload fields (`body`, `headers`, `payload`, `rawBody`, `requestBody`, and `responseBody`), deterministic filtered Mermaid rendering, stable route-id graph correlation, and public helpers/types including `isStudioLiveEvent(value)`, `StudioLiveEvent`, `StudioLiveSnapshot`, and `StudioRequestTrace`; [`docs/reference/toolchain-contract-matrix.md`](./reference/toolchain-contract-matrix.md) carries the canonical CLI scaffolding and inspect artifact output contracts, including that `--timing` defaults to JSON when no explicit output mode is selected; and [`docs/reference/package-surface.md`](./reference/package-surface.md) records the package responsibility split between `@fluojs/cli` artifact emission and `@fluojs/studio` live devtool/artifact viewing/rendering, including the CLI Node.js runtime floor, inspect/timing/report ownership, Node-resolved packaged viewer asset, and legacy standalone timing diagnostics support.

## Cron Migration Option Boundary

The Cron NestJS migration surfaces record that NestJS `timeZone` becomes fluo `timezone`, while `waitForCompletion` has no direct fluo option because scheduler protection and the in-process running guard always skip overlapping ticks for the same task instance rather than queueing another run. Work that relied on NestJS overlap must move to an application-owned queue or worker, and cross-instance exclusion still requires Redis distributed locking.

## File Structure

| Path | Role |
| --- | --- |
| `docs/CONTEXT.md` | Primary AI orientation summary for the repository. |
| `docs/architecture/` | Framework architecture facts, execution model, platform design, and lifecycle boundaries. |
| `docs/contracts/` | Governance rules, release policy, authoring constraints, and conformance expectations. |
| `docs/guides/` | AI-targeted anti-pattern and decision-reference documents. |
| `docs/getting-started/` | Bootstrap and setup facts for common starting paths. |
| `docs/reference/` | Lookup-oriented tables, glossary terms, package matrices, and support snapshots. |

## Navigation

| Need | Read first | Follow with |
| --- | --- | --- |
| Repository identity and non-negotiable rules | `docs/CONTEXT.md` | `docs/contracts/behavioral-contract-policy.md` |
| Architecture model, request flow, and runtime boundaries | `docs/architecture/architecture-overview.md` | `docs/reference/glossary-and-mental-model.md` |
| Package family lookup or runtime coverage | `docs/reference/package-surface.md` | `docs/reference/package-chooser.md` when selection logic is needed |
| i18n ecosystem bridge compatibility and migration boundaries | `docs/reference/i18n-ecosystem-bridges.md` | `docs/contracts/third-party-extension-contract.md` when authoring a third-party bridge |
| Behavioral guarantees, Changesets release flow, and versioning policy | `docs/contracts/behavioral-contract-policy.md` | `docs/contracts/release-governance.md` |
| Testing requirements and canonical fluo TDD ladder | `docs/contracts/testing-guide.md` | `packages/testing/README.md` and `book/beginner/ch20-testing.md` |
| Public API authoring and documentation baseline | `docs/contracts/public-export-tsdoc-baseline.md` | `docs/contracts/platform-conformance-authoring-checklist.md` |
| CLI inspect output modes and artifact ownership | `docs/reference/toolchain-contract-matrix.md` | `packages/cli/README.md` and `docs/reference/package-surface.md` |
| Bootstrap path or startup sequence facts | `docs/getting-started/quick-start.md` | `docs/architecture/lifecycle-and-shutdown.md` |
| Human learning flow or tutorial material | `book/README.md` | relevant chapters under `book/` |

## Anti-Patterns at a Glance

- Enabling `experimentalDecorators` or `emitDecoratorMetadata`, this violates fluo's standard-decorator baseline.
- Reading `process.env` inside package code, this breaks environment isolation and bypasses `@fluojs/config`.
- Shipping a platform package without the documented adapter seam (`HttpApplicationAdapter` for HTTP transports), this breaks runtime portability and conformance.
- Exposing public exports without TSDoc, this weakens package contracts and reviewability.
- Changing documented behavior in `1.0+` without a major bump, this violates release governance.

Full anti-pattern catalog path: `docs/guides/anti-patterns.md`.
