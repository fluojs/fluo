:root {
  color: #172033;
  font-family: system-ui, sans-serif;
}

body {
  margin: 0;
}

main {
  margin: 0 auto;
  max-width: 48rem;
  padding: 4rem 1.5rem;
}

nav {
  display: grid;
  gap: 0.75rem;
  margin-top: 2rem;
}

button,
a {
  font: inherit;
}
