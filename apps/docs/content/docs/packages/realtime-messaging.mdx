---
title: Realtime & Messaging Packages
description: WebSockets, Socket.IO, events, CQRS, microservices, GraphQL, and scheduling.
---

Realtime and messaging packages help you build flows that are not just synchronous HTTP request/response.

| Package | Use for | Deep dive |
| --- | --- | --- |
| `@fluojs/websockets` | Raw WebSocket gateways | `packages/websockets/README.md` |
| `@fluojs/socket.io` | Socket.IO rooms, namespaces, auth guards | `packages/socket.io/README.md` |
| `@fluojs/event-bus` | In-process events and Redis fan-out | `packages/event-bus/README.md` |
| `@fluojs/cqrs` | Commands, queries, events, sagas | `packages/cqrs/README.md` |
| `@fluojs/microservices` | Multi-transport service runtime | `packages/microservices/README.md` |
| `@fluojs/graphql` | GraphQL resolvers, schemas, DataLoaders | `packages/graphql/README.md` |
| `@fluojs/cron` | Cron, interval, timeout triggers | `packages/cron/README.md` |

`@fluojs/event-bus` uses `EventBusService.publish(event, options?): Promise<EventPublishResult>` for best-effort publication and result observation. Check `settled` and every outcome together; `no-recipients` or an empty result is not success for a required reaction. Observation covers only matching local handlers and outbound transport channels. An adapter success with zero subscribers is still transport success, not evidence of remote processing or durability.

Results and bus logs omit handler return values, but app-owned logs remain the application's responsibility. Awaited timeout/cancellation does not terminate started work. Background completion awaits actual work and ignores timeout and post-start cancellation, so it can remain pending after bounded shutdown and be lost on process exit. Follow the [package README](https://github.com/fluojs/fluo/blob/main/packages/event-bus/README.md) for the full contract and the [messaging guide](/docs/guides/messaging-workflows#best-effort-versus-required-results) for the comparison between post-authentication best-effort bookkeeping and required-result checks.

Read [Realtime](/docs/guides/realtime) or [Messaging & Workflows](/docs/guides/messaging-workflows) based on whether the client connection or the backend workflow is the main concern.
